package java8;

import gs.SearchTree;

import java.util.*;
import java.util.function.Function;
import java.util.function.Supplier;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class SapientTest {
    public static void main(String[] args) {

       /* String str = "kapilarora";
        Map<String,Long> map  = Arrays.stream(str.split("")).collect(Collectors.groupingBy(Function.identity(),Collectors.counting()));

        map.entrySet().forEach(e -> {
            System.out.println(e.getKey() + " " + e.getValue());
        });*/

        // List<String> list = Arrays.asList("aa,bb,cc,aa,aa");

        /*list.stream().filter(e -> {
            System.out.println("Inside filter");
            e.equalsIgnoreCase("aa");
            return true;
        }).map(m ->{
            System.out.println("Inside map");
            return m.length();
        }).forEach(System.out::println);
*/

    }
}
